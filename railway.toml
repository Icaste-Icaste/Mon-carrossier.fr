[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npx serve -s . -l $PORT --no-clipboard"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
